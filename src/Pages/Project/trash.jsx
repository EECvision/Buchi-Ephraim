      {/* <nav
        style={activeContentId !== "0" ? { color: "black" } : {}}
        className={classes.nav}
      >
        <div onClick={() => handlePage({ page: "", handleSetState, navigate })}>
          <i className="fas fa-arrow-left"></i> <span>Home</span>
        </div>
        <div
          onClick={() =>
            handlePage({ page: "about", handleSetState, navigate })
          }
        >
          About
        </div>
      </nav> */}

      // useEffect(() => {
      //   let container = document.getElementById("transparent");
      //   container.scrollIntoView();
      // }, [params.id]);