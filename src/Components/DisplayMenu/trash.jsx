  
  
  // const handleSlideLeft = () => {
  //   if (hold) return;
  //   if (slideActiveCount <= 0) return;
  //   setSlideActiveCount((sc) => sc - 1);
  // };

  // const handleSlideRight = () => {
  //   if (hold) return;
  //   if (slideActiveCount >= 3) {
  //     setReset(true);
  //     setSlideActiveCount(0);
  //   } else {
  //     setReset(false);
  //     setSlideActiveCount((sc) => sc + 1);
  //   }
  // };

  // const DisplayMenu = ({ data, setInview, hold }) => {


  // const [slideNumberOfCounts, setSlideNumberOfCounts] = useState(1);

  // const [reset, setReset] = useState(false);

/* <div
          onClick={handleSlideLeft}
          className={`${classes.left} ${slideActiveCount && classes.active}`}
        >
          Left
        </div>
        <div
          onClick={handleSlideRight}
          className={`${classes.right} ${
            slideActiveCount !== slideNumberOfCounts && classes.active
          }`}
        >
          Right
        </div> */
